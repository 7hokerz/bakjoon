#include <iostream>
#include <stack>
using namespace std;
/*
선언 방법: stack <데이터타입> 이름 

stack.push(element): 스택에 데이터 추가
*stack.pop(): 스택의 top 데이터 삭제
*stack.top(): 스택 상단의 원소를 반환
stack.size(): 스택의 크기 반환
stack.empty(): 스택이 비어있으면 1, 아니면 0 반환

(* 표시된 함수들은 스택이 빈 상태에서 사용시 에러 발생)

swap(stack1, stack2): 두 스택의 내용을 서로 바꾼다.

**stack도 결국 클래스니까 객체 배열, 객체 포인터 등 여러가지를 써먹을 수 있다는 걸 참고하자.

*/
int main(){
    stack <int> stack;
    
}