#include <iostream>
#include <stack>
#include <queue>
#include <vector>
#include <string>
#include <cstring>
#include <algorithm>
#include <cmath>
#include <math.h>
#include <stdlib.h>
#include <set>
#include <map>
using namespace std;
typedef long long ll;
typedef pair<int,int> pii;
typedef pair<ll, ll> pll;
/*
set, map 
1. 모두 이진 트리로 구성되어 있음.
노드의 왼쪽에는 자신보다 우선순위가 작은 것을, 오른쪽에는 자신보다 우선순위가 높은 것을 놓음.

2. 형태
map >> key, value
set >> key
(key는 중복될 수 없음!)

3. 사용법
insert: 규칙에 맞게 데이터들이 삽입됨
erase: 규칙에 맞게 데이터들이 삭제됨

begin: 루트 노드를 가리킴(front)
end: 데이터의 끝을 가리킴(back)

find(): 데이터의 위치를 찾음

lower_bound: 찾고자 하는 key 값 이상인 것들 중 가장 작은 key값을 찾음
upper_bound: 찾고자 하는 key 값 초과인 것들 중 가장 작은 key값을 찾음

empty: 데이터 존재 유무
size: 데이터 개수
clear: 초기화

*/
set<string> s;
map<string, int> m;

int main(){ ios::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
    
}   
/*


*/ 